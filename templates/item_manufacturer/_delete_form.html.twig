<form method="post" action="{{ path('item_manufacturer_delete', {'id': item_manufacturer.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ item_manufacturer.id) }}">
    <button class="btn">Delete</button>
</form>
